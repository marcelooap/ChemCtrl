/**
 * Re-exporta o guard compartilhado (path legado do módulo industrialização).
 */
export { useSubmitGuard } from '@/shared/hooks/useSubmitGuard';
export { useSubmitGuard as default } from '@/shared/hooks/useSubmitGuard';
